# LabNexus 产品上下文(自包含摘要)

> 供开发与 AI 接手使用。**完整 PRD 位于仓库外 `../research-group-app/PRD.md`(活文档,产品决策以它为准)**;本文是 labnexus 目录内自包含的摘要,防止新接手方因读不到外部目录而丢失上下文。

## 1. 产品定位

**一句话**:课题组自己的科研朋友圈 + 知识库 + 进度监督平台。

**三大需求**(原始痛点):
1. **资源库**:文件/链接统一入库,支持标签、描述、下载与浏览器内预览(PDF/图片/文本/视频);论文以 PDF 形式作为文件上传(paper 类型与 DOI/arXiv 抓取已废弃,见 specs/f8-paper-meta.md);
2. **笔记与社区**:个人笔记空间(自定义目录)+ 可公开帖子(朋友圈/论坛式,点赞评论);
3. **项目进度**:任务看板 + 状态监督 + 超期提醒(解决拖延)。

**用户**:10 人以内高校课题组。角色三枚举 `admin / supervisor(导师) / student`,注册默认 student。**纯内部私有平台,无访客/公网角色**。

**市场结论**(调研依据,见外部 PRD §1.2):"文献+笔记+帖子+任务"四合一私有科研社区,商业/开源均无现成产品——这是自建的核心机会。

## 2. 功能清单

### 开发目标(F1~F9)

| 编号 | 功能 | 优先级 | 阶段 |
|---|---|---|---|
| F1 | 账号:邀请码注册 + 登录 + JWT | P0 | 1 |
| F2 | 个人空间:目录树管理 | P0 | 1 |
| F3 | 文档(笔记=帖子):创建/编辑 + 可见性切换 | P0 | 1 |
| F4 | 社区信息流:时间线 + 点赞 + 评论 | P0 | 1 |
| F5 | 标签系统:创建 + 打标 + 筛选 | P0 | 1 |
| F6 | 基础搜索(标题/内容 LIKE) | P0 | 1 |
| F7 | 资源库:文件/链接 + 标签 + 检索(论文以 PDF 文件上传) | P1 | 2 |
| F8 | 文献元数据:DOI/arXiv 自动抓取(**已废弃,重做后再引入**) | — | — |
| F9 | 项目/任务:看板 + 状态机 + 里程碑 | P1 | 2 |
| F10 | 经费管理:周转批次/明细/上交/资金池(仅 admin+导师可见,PRD-经费管理.md) | P0 | 3 |

### 灵感库(F11~F21,暂不实现)

F11 文档↔资源关联 / F12 周报 / F13 通知推送 / F14 微信机器人 / F15 全文搜索升级 / F16 文档任务互转 / F17 导师视图 / F18 社交增强 / F19 积分激励 / F20 知识图谱 / F21 编辑器内嵌引用块。
每条有触发条件(见外部 PRD §4.4),**触发条件满足且经人工确认后才可捞回**。

## 3. 核心设计(理解代码的关键)

1. **统一内容模型(最重要)**:`Document` 是唯一内容实体,`visibility: private/public` 切换 —— **私有 = 笔记,公开 = 帖子**,评论/点赞挂在 Document 上。这是与 Notion+论坛式分离设计最大的区别,也是"文献↔笔记↔帖子↔任务"关联能成立的基础。
2. **权限单点**:所有权限判断收敛到 `can(user, action, target)` 函数;role 三枚举为 supervisor 差异化权限预留(P2 在函数内加分支)。
3. **模块化单体 + MVC 轻量分层**:`internal/<domain>/{handler,service,repository}`,依赖方向 handler→service→repository;跨模块调用仅限 service 层;构造函数注入,无全局状态。
4. **数据模型**:权威定义 `docs/schema.sql`(阶段 1/2/3 表已齐全;`resource_refs`/`weekly_reports` 暂缓)。
5. **任务状态机**:`todo → in_progress → blocked → todo`、`in_progress → done`;流转校验在 service 层(`TaskService.Transition`)。

## 4. 已定决策清单(勿推翻;推翻需先改外部 PRD §9)

| # | 决策 | 一句话理由 |
|---|---|---|
| D1 | 统一内容模型(笔记=帖子) | 核心差异化 |
| D2 | 三角色 + `can()` 权限接口 P0 落地 | 导师差异化权限留口 |
| D3 | 不设访客/只读角色 | 纯内部平台 |
| D4 | 模块化单体,不做微服务 | 10 人规模,1 人开发 |
| D5 | P0 = 社区四件套+标签+搜索 | 先让社区活 |
| D6 | F10~F20 进灵感库,开发目标仅 F1~F9 | 聚焦 |
| D7 | Go+Gin+GORM+JWT(Redis refresh)/React+Vite+Tailwind+**shadcn/ui**(正式前端独立仓库 labnexus-frontend) | 用户熟悉 Go 栈 + AI Coding 友好;shadcn 与 Tailwind 零冲突、视觉现代、源码可定制(2026-08-25 定) |
| D8 | 部署三阶段:本地→0 成本演示→国内服务器 | 先演示确认再投入 |
| D9 | 引用格式默认 GB/T | 中国高校惯例 |
| D10 | 产品代号 LabNexus | 已定 |
| D11 | MVC 轻量分层,不用 DDD | 用户未学 DDD,保持简单 |

## 5. 防跑偏清单(明确不做)

- ❌ 微服务、DDD、实时多人协编、原生 App、私聊 IM、公网开放、日历/审批流;
- ❌ 灵感库 F10~F20(除非触发条件满足 + 人工确认);
- ❌ 引入新框架/新依赖/重写架构(先讨论,standards §7.3);
- ❌ 修改 `schema.sql` / `api-contract.md` / `standards.md` 不经过 review;
- ❌ 跳过测试、绕过分层、权限硬编码。

## 6. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段 0 | 骨架 + 规范 + 契约 + schema | ✅ |
| 阶段 1 | F1~F6 社区 MVP | ✅ 已完成 |
| 阶段 2 | F7~F9 资源库 + 任务 | ✅ 已完成 |
| 阶段 3 | 迭代 + 导师参与 | ⏳ |

## 7. 文档索引

| 文件 | 内容 |
|---|---|
| `AGENTS.md` | AI 接手指南(入口) |
| `docs/standards.md` | 开发规范(SDD+TDD+AI 工作流) |
| `docs/api-contract.md` | API 契约 v0.1(F1~F9 + 管理端) |
| `docs/schema.sql` | 数据模型权威定义 |
| `docs/specs/_template.md` | 功能规格模板 |
| `docs/specs/f1-auth.md` | F1 规格(已评审,待实现) |
